{
  "private": true,
  "type": "module",
  "scripts": {
    "format-app": "npx prettier --write app/",
    "format-resources": "npx prettier --write resources/",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.22.4",
    "husky": "^8.0.0",
    "lint-staged": "^15.4.3",
    "prettier": "^3.5.3"
  }
}
